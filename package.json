{
  "name": "root",
  "private": true,
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "start": "npm run start -w packages/library",
    "build": "npm run -ws build",
    "clean": "npm run -ws clean"
  },
  "devDependencies": {
    "@types/node": "22.16.0",
    "rimraf": "4.4.1",
    "typescript": "5.6.2"
  }
}
